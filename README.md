# terraform-google-external-https-lb
Terraform module for a GCP external HTTPS load balancer in front of a single Cloud Run service (static IP, Serverless NEG, backend, URL maps, managed SSL, HTTP redirect, uptime checks, monitoring dashboard).
